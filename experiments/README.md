# Experiment configs and results

This directory stores experiment configurations and their outputs.

## Structure

```
experiments/
├── configs/       # YAML/JSON experiment definitions
├── results/       # Raw experiment outputs
└── analysis/      # Notebooks and analysis scripts
```

Experiments are created and managed via `sparsify experiment` CLI commands (Sprint 4).
